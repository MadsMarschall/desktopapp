/* eslint-disable */

import DataOperationProxy from '../../renderer/DataHandling/DataOperationProxy';

let dop: DataOperationProxy;

beforeEach(() => {
  dop = new DataOperationProxy("test-id");
});

